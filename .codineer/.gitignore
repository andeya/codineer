# Tracked (committed to the repo)
!CODINEER.md
!settings.json
!plugins/
!skills/

# Ignored (local / runtime artifacts)
settings.local.json
sessions/
agents/
sandbox-home/
sandbox-tmp/
todos.json
cache/
